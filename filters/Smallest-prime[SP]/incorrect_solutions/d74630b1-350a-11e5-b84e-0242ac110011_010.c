/*compile-errors:*/
/*compile-result:1*/
/*save-event:compile*/
#include<stdio.h>
int p;
int i;
int N;

int check_prime(int N){
    scanf("%d",&N);
    p=N;
    while(p==N){
        i=2;
        while((i==2)&&(i<p)){
            if(p%i!=0){
            i=i+1;
                      }}
            p=p+1 ;}
            return p;
    
}
            
  

int main(){
 scanf("%d",&N);
 int check_prime(int N);
 printf("%d",p);
 
    return 0;
}

