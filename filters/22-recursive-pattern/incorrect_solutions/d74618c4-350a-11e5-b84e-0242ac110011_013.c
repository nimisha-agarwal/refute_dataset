/*compile-errors:sh: 1: exec: clang: not found*/
/*compile-result:1*/
/*save-event:compile*/
#include <stdio.h>

int main(){
    int n;
    scanf("%d",&n);
    int temp=n-5;
    int pat(int n,int a[],int temp)
    {
        a[0]=n;
        for(i=1;i<n;i++)
        printf("%d",n-5);
        return n-5;
}
	return 0;
}